# PRIMEIRAMENTE INSTALEI ALGUMAS BIBLIOTECAS:
Usei o pip install no terminal para cada um das seguintes bibliotecas:

1. **pyautogui**: Usado para realizar as funções de clicar e escrever.
2. **pillow**: Usado para realizar uma variedade de tarefas relacionadas a imagens,
como redimensionar, recortar, girar, aplicar filtros, ajustar a qualidade e o formato de uma imagem, entre outros.
3. **mouseinfo**: Usado para descrobrir coordenadas na tela, da seguinte mandeira, escreva no terminal:
    1. python
    2. from mouseinfo import mouseInfo
    3. mouseInfo()

# PASSO A PASSO PARA ENCONTRAR A COORDENADA DO APLICATIVO
Para que você consiga clicar em um elemento específico da tela, deverá achar a coordenada de tal elemento,
e para que isso seja possível, precisamos ir no terminal e digitar na seguinte ordem:

1. python
2. from mouseinfo import mouseInfo
3. mouseInfo()
4. encontrar a coordenada do elemento que deseja e clicar a tecla "F6" para salvar a coordenada

# TRASNFORMAR O ARQUIVO EM EXE.
1. Criar um ambiente virtual: python -m venv env
2. Ativar o ambiente: env/Scripts/activate
3. Esperar o arquivo ativar com o simbolo do sifrão $ e aparacer o (env)
4. Instalar: pip install auto-py-to-exe
5. Digitar no terminal e dar um enter: auto-py-to-exe 
6. Terminar configurações no pop-up que abrirá

### Lembrando de sempre verificar que instalou todas as bibliotecas do código